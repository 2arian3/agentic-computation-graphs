# Extra dependencies for the dashboard backend (on top of the instrument's
# requirements.txt at the repo root). Install into the same .venv:
#   ./.venv/bin/pip install -r webapp/requirements.txt
fastapi>=0.110,<1
uvicorn[standard]>=0.27
sse-starlette>=1.8
