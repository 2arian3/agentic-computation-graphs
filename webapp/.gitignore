# Frontend build + deps
frontend/node_modules/
frontend/dist/
frontend/.vite/

# App-owned runtime state (regenerated at runtime)
data/history/

# stray package-lock churn is fine to keep; ignore editor cruft
*.log
